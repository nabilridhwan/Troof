export enum STATUS {
	IN_LOBBY = "in_lobby",
	IN_PROGRESS = "in_progress",
	FINISHED = "finished",
}

export enum Type {
	TruthOrDare = 1,
	WouldYouRather = 2,
}

export enum ACTION {
	WAITING_FOR_SELECTION = "waiting_for_selection",
	TRUTH = "truth",
	DARE = "dare",
}
